export interface Modules {
    id: number;
    url: string;
    value: string;
    color: string;
    icon: string;
    code: string;
}

export interface LoginForm {
    username: string,
    password: string
}